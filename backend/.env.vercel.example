# v1 local-workbook mode on Vercel
LOCAL_XLSX_PATH=artwork.xlsx
ALLOWED_ORIGINS=https://your-frontend-project.vercel.app

# Optional override (auto-detected on Vercel if omitted)
# STATIC_ROOT=/tmp/static

